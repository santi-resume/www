# Resume

```bash
nvm use node
npm i
npm run dev
```

For the pdf, click "open" all the sections then press to "print" and save as PDF!

There is a `pre-push` hook in the  `.git/hooks` which will build the docs folder.
